type Query {
    shows(personList: [Person]): Show
    show(people: Person): Show
    showWithGood(good: Good): Show
}

#type Mutation {
#}

type Show {
    id: ID!
    name: String
}

input Person {
    name: String
}

input Good {
    name: String
}

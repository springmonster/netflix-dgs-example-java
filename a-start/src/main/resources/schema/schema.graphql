type Query {
    shows(titleFilter: String): [Show]
    showsWithDgsData: [Show]
    actors: [Actor!]
    user: User!
    movies: String
    games: String
}

type Mutation {
    addUser(id:String!, name:String!): User!
}

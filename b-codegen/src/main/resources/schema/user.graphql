type User {
    id: Int!
    name(className: String): String
    age: Int
    score(subject: SubjectEnum): [Score]
}

type Score {
    score: Int
    subject: SubjectEnum
}

input Page {
    pageNo: Int
    pageSize: Int
}

input UserInput {
    name: String
    age: Int
}